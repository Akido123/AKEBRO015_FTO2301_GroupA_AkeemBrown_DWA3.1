## Code Snippet

```javascript
// Define a function to calculate the square of a number
function calculateSquare(num) {
    return num * num;
}

// Call the calculateSquare function with an argument
const result = calculateSquare(5);

console.log(result);